// TODO: Watch library folder for changes (additions, deletions, modifications) and enqueue file events
// using the existing fileEventQueue. This will allow real-time updates to the library without needing
// to rescan the entire directory.
